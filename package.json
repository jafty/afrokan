{
  "name": "afrokan-blog",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "clean": "rm -rf dist",
    "dev": "python3 -m http.server 8000 --directory dist"
  }
}
